const reactnativesvg = {
	G: jest.fn(),
	Text: jest.fn(),
	TSpan: jest.fn(),
	Path: jest.fn(),
	Polyline: jest.fn(),
	Rect: jest.fn()
};
export const { G, Text, TSpan, Path, Polyline, Rect } = reactnativesvg;
